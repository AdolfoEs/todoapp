# Copia este archivo a .env y reemplaza con tus valores reales.
# No subas .env a Git (está en .gitignore).

# URL base de tu app (sin barra final)
# Local: http://localhost:3000
# Producción (Railway): https://tu-app.up.railway.app
BASE_URL=http://localhost:3000

# SMTP para enviar correos de recuperación de contraseña
# Gmail: smtp.gmail.com, puerto 587, y usa "Contraseña de aplicación" (no la contraseña normal)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-correo@gmail.com
SMTP_PASS=tu-contraseña-de-aplicacion
